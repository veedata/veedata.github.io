---
layout: default
---
<div class="page-header">
  <h1>{{ page.title }}</h1>
  {%- if page.description -%}<p class="lead">{{ page.description }}</p>{%- endif -%}
</div>

<div class="prose">
  {{ content }}
</div>
